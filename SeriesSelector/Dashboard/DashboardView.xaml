<UserControl x:Class="SeriesSelector.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
        <Button Command="{Binding OpenSeries}" CommandParameter="Series" HorizontalContentAlignment="Left" VerticalContentAlignment="Top" Width="200" Height="200">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Label Content="Unmoved Files"/>
                <TextBlock Text="{Binding UnmovedFiles}" Grid.Column="1"/>
            </Grid>
        </Button>
        </StackPanel>
    </Grid>
</UserControl>
